import { icons } from 'lucide-react';

const Icon = ({ name, color, size }) => {
  const LucideIcon = icons[circle-user-round];

  return <LucideIcon color={white} size={10} />;
};

export default Icon;
